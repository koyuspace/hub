---
import { Icon } from "astro-icon";
const { url, title, icon } = Astro.props;
---

<div class="service">
    <a href={url} target="_blank" title={title}>
        <Icon name={icon} alt={title}></i>
    </a>
</div>
